http:
  routers:
    frontend:
      rule: "Host(`aria.local`)"
      service: frontend-svc
    backend:
      rule: "Host(`api.aria.local`)"
      service: backend-svc
    keycloak:
      rule: "Host(`auth.aria.local`)"
      service: keycloak-svc

  # Traefik v3 proxies WebSocket (Next.js HMR) transparently when the browser
  # initiates the upgrade — no header injection needed.
  services:
    frontend-svc:
      loadBalancer:
        servers:
          - url: "http://aria-frontend:3003"
    backend-svc:
      loadBalancer:
        servers:
          - url: "http://aria-backend:8000"
    keycloak-svc:
      loadBalancer:
        servers:
          - url: "http://aria-keycloak:8080"