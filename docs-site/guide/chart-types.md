# Supported Chart Types

ARIA picks a chart that fits your question, rendered from structured JSON (recharts):

- **Bar / Column** — comparisons across categories.
- **Line / Area** — trends over time.
- **Pie / Donut** — share of a whole.
- **Table** — raw rows (subject to the row limit).

You can switch the palette and, where enabled, export the underlying data.
