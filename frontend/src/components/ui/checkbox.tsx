// Adopted from @b2m-team/ui (shadcn HSL tokens, bridged in globals.css).
// Re-exported here so existing `@/components/ui/checkbox` imports keep working.
export { Checkbox, type CheckboxProps } from "@b2m-team/ui";
