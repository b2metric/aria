// Adopted from @b2m-team/ui (shadcn HSL tokens, bridged in globals.css).
// Re-exported here so existing `@/components/ui/button` imports keep working.
export { Button, buttonVariants, type ButtonProps } from "@b2m-team/ui";
