// Adopted from @b2m-team/ui (shadcn HSL tokens, bridged in globals.css).
// Re-exported here so existing `@/components/ui/card` imports keep working.
export {
  Card,
  CardHeader,
  CardTitle,
  CardDescription,
  CardContent,
  CardFooter,
} from "@b2m-team/ui";
