# Sprint 11.2: System Cohesion Fixes Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use engineering-core:subagent-driven-development (recommended) or engineering-core:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Unify the "Team Conventions" UI with real PostgreSQL Teams and expand Audit Logs to trace the full Conversation Logic (NL question + SQL + explanation).

**Architecture:** 
- Fetch real teams from `/api/admin/teams` inside `/admin/team-memory`.
- Extend `DataAuditLog` `details` payload in `AuditService.log_query` to accept and persist the original user question and the AI explanation.

**Tech Stack:** Python 3.12, FastAPI, SQLAlchemy, Next.js, React.

---

### Task 1: Audit Log Full Trace Backend Support

**Files:**
- Modify: `backend/app/services/audit.py`
- Modify: `backend/app/query/pipeline.py`

- [ ] **Step 1: Extend `AuditService.log_query`**
In `backend/app/services/audit.py`, modify the `log_query` method signature to accept `question: str | None = None` and `explanation: str | None = None`. Add them to the `details` dictionary if they are provided.

- [ ] **Step 2: Pass full context to `_audit` in `pipeline.py`**
In `backend/app/query/pipeline.py` inside `_execute_sql`, modify the nested `_audit(success, row_count, error)` helper function to accept `question: str | None = None` and `explanation: str | None = None`. Pass them to `audit.log_query` and `audit.log_event`.
*Note: `_execute_sql` does not currently receive `question` or `explanation`. Change `_execute_sql` signature to accept them or find a way to pass them from `process_query` where `request.question` is available.*
*Better Approach:* Since `process_query` orchestrates everything, it knows the `question` and `explanation`. It might be cleaner to adjust `_execute_sql` signature to take `question: str` and `explanation: str | None`.

- [ ] **Step 3: Update `_execute_sql` calls**
In `backend/app/query/pipeline.py`, update the call to `_execute_sql()` to pass `question=request.question` and `explanation=explanation`.

- [ ] **Step 4: Commit**
`git add` and `git commit -m "fix(audit): include user question and explanation in query audit logs"`

---

### Task 2: Team Memory UI Real Teams Integration

**Files:**
- Modify: `frontend/src/app/admin/team-memory/page.tsx`
- Modify: `backend/app/api/endpoints/admin/team_memory.py`

- [ ] **Step 1: Backend UUID Validation (Optional but recommended)**
In `backend/app/api/endpoints/admin/team_memory.py`, ensure `team_id` works smoothly. It currently uses `team_id: str = "default"`. Leave it as `str` to not break Mem0 (which accepts strings for user/team IDs), but remove "default" fallbacks where appropriate. *Actually, Mem0 uses `user_id` parameter to store Team memories.* No major backend changes strictly needed, but let's make sure it handles real UUID strings well.

- [ ] **Step 2: Fetch real teams in Frontend**
In `frontend/src/app/admin/team-memory/page.tsx`, add state for `teams`. In `useEffect`, fetch `/api/admin/teams` to get the list of real teams. 

- [ ] **Step 3: Update the UI dropdown**
Replace the hardcoded `<select>` that has `<option value="default">Default Team</option>` with a dynamic select mapping over the fetched `teams`. The option `value` should be `team.id` and the label `team.name`. Provide a loading state if teams are fetching. If no teams exist, show a message "Please create a team in Users & Teams first."

- [ ] **Step 4: Commit**
`git add` and `git commit -m "fix(admin): use real postgres teams in team memory conventions UI"`