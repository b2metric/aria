# Sprint 1.4 Review — JWT Middleware + RBAC

> **Commit:** — | **Tests:** 28/28 ✅ | **Coverage:** 86% | **Files:** 16

## Yapilanlar

| Metrik | Deger |
|--------|-------|
| Test | 28 (hepsi PASS) |
| Coverage | 86% (260 statements) |
| Dosya | 16 |
| Keycloak | Realm config + JWKS + bootstrap |

## Auth Mimarisi

```
Request → Bearer token → JWKS validation (1h cache) → UserContext
                                                          │
                                    ┌─────────────────────┤
                                    ▼                     ▼
                              role check              workspace check
                              (RBAC guard)            (JWT vs path)
```

## Roller ve Permission'lar

| Rol | can_view_sql | can_manage_team | can_manage_workspace | can_admin |
|-----|-------------|-----------------|---------------------|-----------|
| admin | ✅ | ✅ | ✅ | ✅ |
| team_lead | ❌ | ✅ | ❌ | ❌ |
| analyst | ✅ | ❌ | ❌ | ❌ |
| viewer | ❌ | ❌ | ❌ | ❌ |

## Endpoint Koruma

| Endpoint | Erisim |
|----------|--------|
| GET /health | Public |
| GET /me | Authenticated |
| GET /workspace/{id}/info | Authenticated + workspace check |
| GET /admin/dashboard | Admin only |
| GET /team/manage | Team lead+ |
| GET /queries/sql-preview | can_view_sql (admin + analyst) |
| GET /workspace-scoped/query | Authenticated (workspace_id injection) |

## Keycloak Config

- Realm: `aria-realm.json` (Keycloak 26)
- Client'lar: backend (SA), web (PKCE), cli (CC)
- Rol: admin, team_lead, analyst, viewer
- Claims: workspace_id, user_id, team_id, role
- JWKS: 1 saat cache, kid mismatch'te force refresh

## Kararlar

- [x] python-jose 3.5 (leeway options dict icinde)
- [x] JWT header → HTTPException (MissingTokenError yerine)
- [x] can_view_sql = admin + analyst
- [x] RBAC: require_role(Role.X) function factory + Depends()
- [x] workspace_id: JWT claim vs path karsilastirmasi

## Review Notlari

### ✅ Iyi
- 28/28 test pass, 86% coverage saglam
- Keycloak realm config temiz, bootstrap.sh ile otomasyon
- RBAC guard pattern temiz (function factory)
- workspace isolation JWT + path check guvenli

### ⚠️ Duzeltilecek

1. **can_view_sql scope**: Su an "admin + analyst" goruyor. Ama admin settings'te `show_sql_to_roles: ["admin", "team_lead"]` default'u vardi. Team lead SQL gormeyecek mi?
   → Karar: **team_lead de gorsun** (admin config ile override edilebilir)

2. **Permission mapping**: can_view_sql dogrudan rol uzerinden, user-level `users.can_view_sql` override'i eksik
   → Sonraki sprint'te ekle

3. **Token refresh**: Access token 15min, refresh 8h var mi?
   → Keycloak config'te belirtilmemis, eklenmeli

## Onerilen Aksiyon

1. `can_view_sql` scope'a `team_lead` ekle
2. `users.can_view_sql` per-user override (sonraki sprint)
3. Keycloak token expiry: access 15min, refresh 8h
