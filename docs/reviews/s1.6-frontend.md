# Sprint 1.6 Review — Next.js Frontend Skeleton

> **Repo:** b2metric/aria | **Branch:** main | **Framework:** Next.js 16 + shadcn/ui v4

## Yapilanlar

| Metrik | Deger |
|--------|-------|
| Dosya | 58 |
| LOC | 13,917 |
| Route | 7 |
| Build | ✅ PASS |
| TypeCheck | ✅ PASS |

## Route'lar

| Route | Sayfa | Durum |
|-------|-------|-------|
| /login | Login form (ARIA branding) | ✅ mock auth |
| /dashboard | Quick stats (4 kart), recent queries, right panel | ✅ |
| /chat | Sidebar + mesaj alani + input bar | ✅ |
| /admin | Tab'lar: customers(3), databases(3), teams(3), users(4) | ✅ |
| /schema | Placeholder | ⚠️ bos |
| /api/[...path] | BFF proxy → FastAPI backend | ✅ |

## Tech Stack

| Katman | Secim |
|--------|-------|
| Framework | Next.js 16.2.7 (App Router) |
| UI Kit | shadcn/ui 4.x (@base-ui/react primitives) |
| CSS | Tailwind CSS v4 |
| Ikon | lucide-react |
| Tema | next-themes (dark/light) |
| Auth | Mock (lib/auth.ts) — Keycloak'a hazir |

## Bilesenler

| Grup | Bilesen |
|------|---------|
| Auth | login-form, user-nav |
| Layout | app-sidebar, breadcrumb-header |
| UI | theme-toggle |

## Review Edilecek Noktalar

### 1. shadcn/ui v4 — base-ui primitives

- shadcn/ui v4, Radix yerine @base-ui/react kullaniyor
- `asChild` pattern kalkmis, yerine render prop
- Sidebar navigasyonu icin `<Link>` wrapper kullanilmis
- ⚠️ base-ui API'si degisebilir, breaking change riski var

### 2. Mock Auth → Keycloak Gecisi

- Su an `lib/auth.ts` mock kullanicilar: fatima (analyst), admin
- Keycloak OIDC entegrasyonu icin hazir yapi
- JWT'den role/team/workspace extraction yapilmasi lazim
- ⚠️ Token refresh, session management henuz yok

### 3. BFF Proxy

- `/api/[...path]` → FastAPI backend'e proxy
- Hata handling, timeout, retry logic?
- ⚠️ Backend hazir olana kadar mock response lazim

### 4. Eksik Sayfalar

- /schema placeholder — bos
- /analytics — yok (admin dashboard'da olmali)
- /admin/memory — yok (memory manager)
- /admin/tenant-config — yok (per-tenant config)
- Mobile responsive — test edilmemis

### 5. Test

- Unit test yok
- E2E test yok
- Vitest config var ama test dosyasi yok

## Kararlar

- [x] Next.js 16 (latest stable)
- [x] shadcn/ui v4 base-ui primitives
- [x] Mock auth → Keycloak migration ready
- [x] BFF proxy pattern
- [x] Dark/light theme support

## Onerilen Aksiyonlar

1. `@/lib/auth.ts`'i Keycloak OIDC'ye migrate et (Sprint 1.4 ile birlikte)
2. **Token refresh: Silent refresh (A)** — access 15min expire olunca refresh token ile arkada yenile, kullanici fark etmesin
3. `/schema`, `/analytics`, `/admin/memory`, `/admin/tenant-config` sayfalari ekle (Sprint 2)
4. BFF proxy'ye error handling + timeout ekle
5. Vitest ile ana sayfa render testleri ekle
6. **Responsive**: Tum sayfalar mobile-first responsive olmali (Sprint 2)
7. **base-ui abstraction**: UI bilesenleri facade pattern arkasinda, swappable (Sprint 2)
